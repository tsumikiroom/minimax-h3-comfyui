# ComfyUI のモデル置き場構成(2026-08-04 調査・移行)

MiniMax H3 の 42.5GB を落とす過程で構成を洗い出したので、その記録。この slug 固有の話ではなく
ComfyUI 環境全体の話だが、調べたのがここなので当面ここに置く。

## ドライブ

| | 実体 | 容量 / 空き | 実測シーケンシャル読み出し |
|---|---|---|---|
| C: | NVMe PC801 SK hynix 2TB(内蔵) | 1888 GB / 335 GB | **1,095 MB/s** |
| D: | **Samsung PSSD T7 Shield(USB外付け)** | 1863 GB / 1594 GB | **442 MB/s** |
| G: | Google Drive | — | — |

**差は約2.5倍**。スペックから想像するほどの差ではない(21GB のモデルで初回ロード 約19秒 → 約48秒)。
外付け運用を避ける理由にはならないと判断した。

## Desktop 2 の構成(移行前)

```
D:\ComfyUI\Comfy Desktop\        アプリ本体(ToDesktop 管理。更新で丸ごと差し替わる)
C:\Users\kawam\ComfyUI-Installs\ComfyUI\ComfyUI\   コア本体(git checkout)  0.1 GB
C:\Dev\ComfyUI_app\              basePath
  ├── .venv\                     4.7 GB
  ├── custom_nodes\              0.3 GB
  ├── models\                    138.8 GB  ← 全体の96%
  ├── input\ output\             0.3 GB
C:\Users\kawam\ComfyUI-Shared\models\   設定上は is_default だが **空・未使用**
```

**⚠️ `D:\ComfyUI\Comfy Desktop\` にモデルを置いてはいけない。** アプリ更新で消える領域。
同じ D: なら `D:\ComfyUI\models`(アプリ本体と同階層・別フォルダ)にする。

## モデルパスの正規の変更方法

`%APPDATA%\Comfy Desktop\shared_model_paths.yaml` は **「Generated by Comfy Desktop — do not edit manually」**
と書かれた自動生成ファイル。直接編集しない。

正は `%APPDATA%\Comfy Desktop\settings.json` の **`modelsDirs` 配列**。ここを変えると yaml が再生成される。

```json
"modelsDirs": [
  "C:\\Users\\kawam\\ComfyUI-Shared\\models",   ← 空・未使用。この機に削除
  "C:\\Dev\\ComfyUI_app\\models"                ← D:\ComfyUI\models へ差し替え
]
```

編集は **ComfyUI Desktop を終了した状態で**行う(起動中だと上書きされる)。

## 移行(2026-08-04 完了)

`C:\Dev\ComfyUI_app\models` → `D:\ComfyUI\models` へ 138.8GB(60ファイル)。**完了・検証済み**。

結果: C: 空き **334GB → 471GB**(+137GB)。D: 138.8GB / 60ファイル。
移行後もローダーの認識数は変化なし(UNETLoader 2 / CLIPLoader 3 / VAELoader 4 / UnetLoaderGGUF 6 / LoraLoaderModelOnly 4)。

- **移動ではなくコピー**で実施。D: に1594GB空きがあるので、認識確認が済むまで C: 側を残すほうが安全
- モデルは読み取り専用なので ComfyUI 起動中でもコピー自体は安全
- 実測スループット 約350 MB/s

```powershell
robocopy "C:\Dev\ComfyUI_app\models" "D:\ComfyUI\models" /E /COPY:DAT /DCOPY:DAT /R:2 /W:5 /J /NFL /NDL /NP /LOG:<log>
```

`/J` は非バッファI/O。大きいファイルが主体なので有効。実測 616 MB/s。

**⚠️ robocopy の終了コード 1 は成功**(ファイルをコピーした)。0〜7 が正常、8以上がエラー。
シェルは非ゼロを失敗と解釈するので、ログの `FAILED` 列を見て判断すること。

### 手順(実際に通ったもの)

1. `robocopy` でコピー(ComfyUI 起動中でも可。モデルは読み取り専用)
2. 相対パス＋サイズで C:/D: を突き合わせて検証
3. **ComfyUI Desktop を終了**(起動中に settings.json を書いても上書きされる)
4. `settings.json` の `modelsDirs` を `["D:\\ComfyUI\\models"]` に差し替え(バックアップを取ってから)
5. 起動 → `shared_model_paths.yaml` が再生成される。ローダーの認識数を API で確認
6. 検証が通ってから C: 側を削除

### 移行後も C: が検索パスに残る

`--base-directory C:\Dev\ComfyUI_app` が自動で `C:\Dev\ComfyUI_app\models\*` を検索パスに足すため、
`modelsDirs` から外しても folder_paths には残る(`modelsDirs` とは別系統)。D: が優先されるので実害なし。
C: 側はファイルだけ削除してディレクトリの骨組みは残した。

### 移行しなかったもの

- `.venv`(4.7GB) — uv 管理で絶対パスが焼き込まれており、移動すると壊れる。
  `installations.json` の `adoptedPythonPath` も C: を指している。4.7GB のために冒すリスクではない
- `custom_nodes` / コア本体 / input / output — 合わせて 0.7GB。移す意味が薄い
