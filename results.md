# 実測結果

> **⚠️ 2026-08-07 システムクラッシュ×2 の教訓(最重要)**
> 同日2回、生成中に PC が完全フリーズし強制電源断(Kernel-Power 41、BugCheck 無し)。
> 2回とも**コミット上限の壁ぎわでページファイルの反応的拡張が間に合わず livelock**。
> - クラッシュ1: `/free` 無しのウォーム連続投入。最終記録 コミット 52.4 / 上限 55.5、物理空き 1.7GB
> - クラッシュ2: 投入前ゲート(余裕25.9GB)は通過したが、25steps 実行中の 18/25 で上限 52.7 に到達
>
> **確立した防御(3層)**:
> 1. **[prestretch.py](prestretch.py)** — 再起動のたびに実行。touch しない commit で境界を踏み抜き、
>    ページファイル上限を先に 66GB+ へ育てる(再起動まで維持)。**崖そのものを消す**。
>    ※ Windows は「上限に近づく」だけでは拡張しない。突き抜ける割り当てで初めて同期拡張する(実測)
> 2. **[killswitch.py](killswitch.py)** — High優先度・GlobalMemoryStatusEx のみの軽量ループ(0.25s)。
>    コミット余裕<2.5GB or 物理空き<0.4GB ×2連続で ComfyUI python を **TerminateProcess で即殺**。
>    実行1本を犠牲に OS を守る。PowerShell+CIM の監視や HTTP /interrupt は livelock 時に動けない(2敗)
> 3. **投入前ゲート**(run_i2v.py 内蔵) + 実行間の `/free`
>
> 起動手順: 再起動 → prestretch.py → killswitch.py 常駐 → ComfyUI 起動 → テスト

## 1. スモークテスト(2026-08-04)

**結論: 動いた。音声つき動画がローカルで生成できることを確認。**

投入は [run_smoketest.py](run_smoketest.py)(公式テンプレートのサブグラフを API 形式に展開したもの)。
出力: [results/MiniMax_H3_smoketest_00001_.mp4](results/MiniMax_H3_smoketest_00001_.mp4)

### 条件

| 項目 | 値 |
|---|---|
| diffusion | `minimax_h3_fl2va_pruned_fp8_scaled`(公式既定の int8_convrot ではない) |
| text encoder | `qwen3vl_32b_minimax_h3_nvfp4_awq` |
| 解像度 / フレーム | 1344×768 / **5**(ノード最小値。`length` は min=5, step=17) |
| steps / sampler / scheduler | **6** / `res_multistep` / `simple` |
| 入力画像 | `Cityscape_NY.jpg` |
| モデル所在 | `D:\ComfyUI\models`(外付け T7 Shield、実測 442 MB/s) |

### 時間

| 項目 | 実測 |
|---|---|
| **合計** | **98.92 秒** |
| モデル初期化(1step目に含まれる) | 27.0 秒 |
| サンプリング 6 steps | 24 秒(**3.8〜4.6 秒/step**) |
| 残り(ロード・VAEデコード・エンコード) | 約 48 秒 |

### メモリ

| 項目 | 実測 |
|---|---|
| **ピーク コミット** | **49.74 GB / 上限 49.7 GB** ← 天井に到達 |
| ピーク VRAM | 15.11 GB / 16.0 GB |
| 最小 利用可能RAM | 1.90 GB |

ComfyUI のステージング量:

```
MiniMaxH3TEModel_   14956 MB
MiniMaxH3VideoVAE    4965 MB
MiniMaxH3           19983 MB
MiniMaxH3AudioVAE     576 MB
```

### 出力の検証(ffprobe)

```
video: h264, 1344x768, 24 fps, 0.208 s (5フレーム)
audio: aac, 32000 Hz, 2ch (ステレオ)
```

**H3 の売りであるネイティブ 32kHz ステレオ音声が実際に出ている**ことを確認。

## 2. 確定した事実 / 覆った想定

- **fp8_scaled は正常に動く**。int8_convrot の追加DL(+21GB)は不要だった。
  ログの `Native ops: float8_e4m3fn, ...` のとおり Ada では fp8 が native。**この選択は結果的に正解**。
- **NVFP4 text encoder はエミュレーション動作**。ログの `emulated ops: nvfp4, mxfp8` が根拠。
  「公式テンプレートの既定値だから Blackwell 専用ではない」は正しかったが、
  「だから Ada でも native に動く」は誤り。**動くが速度ペナルティはある**。
- **mmap でコミットが減るという推測は外れた**。19983MB Staged = ほぼファイルサイズ分を食っている。
  ComfyUI は safetensors を mmap で開くが、dynamic VRAM loading のためにステージングする段で実メモリを確保している。
- **OS再起動でメモリを空ける判断は必須だった**。再起動前の余裕 7.9GB では確実に落ちていた。
  再起動後の 27.4GB でも**ピークで天井(49.74/49.7)に到達**しており、余裕はゼロに近い。

## 3. 運用上の制約(重要)

- **生成中に他のアプリを一切開けない。** コミット余裕が実質ゼロなので、1つ起動しただけで落ちうる。
- 管理者権限が無くページファイルを増やせないため、この制約は解消できない。
- ComfyUI 側も限界を検知している:
  `[MultiGPU_Memory_Monitor] CPU usage (94.0%) exceeds threshold (85.0%)` →
  `Triggering PromptExecutor cache reset`

## 3.5 22フレームの試行(2026-08-04) — **失敗。ただし結論は出ていない**

`run_smoketest.py 22 6` を投入 → **ウォッチドッグが 50.20GB で `/interrupt` を発火して中断**。
history は `status: error`、出力なし。

**重要: これは「22フレームが重い」ことを示していない。**
止まったのは **text encoder のステージング中**で、diffusion のロードにもサンプリングにも到達していない。
フレーム数が効いてくる段階の手前で落ちている。

| | 5フレーム成功時 | 22フレーム試行時 |
|---|---|---|
| 開始時コミット | 22.3 GB | **29.6 GB** |
| ロード段階での到達点 | 49.49 GB | 50.20 GB(中断) |
| 増加量 | 約 20 GB | 約 20.6 GB |

増加量はほぼ同じ。**7.3GB 高いベースラインの分だけ天井を突き抜けただけ**。

### ベースラインが上がった原因

ComfyUI 本体ではない(`/free` 後の python は 4.58GB のみ)。再起動直後には無かった
vmmem(4.01GB)の復帰、138GB のファイルI/Oで膨らんだカーネルプール、自動起動アプリの積み重ね。
→ **有効なテストには OS 再起動が要る。**

### 得られた運用知見

- **ウォッチドッグは有効**。`/interrupt` でシステムを巻き込まずに中断できる。
  実装: [watchdog.ps1 相当](results.md) — コミットを2秒間隔でサンプルし、閾値超えで POST /interrupt
- **`/free` でベースラインは完全に戻る**(`{"unload_models":true,"free_memory":true}`)。
  連続テスト時は毎回挟むこと。挟まないと前回のステージング(24GB)が残る
- **Windows がページファイルを自動拡張している**(上限 49.7 → 50.7 → 51.7GB)。
  ただし拡張は事後的で間に合わない

## 3.6 22フレーム 成功(2026-08-04、OS再起動後の2回目)

**完走。`MiniMax_H3_f22_s6_00001_.mp4`(0.917秒、音声つき)。**

出力: [results/MiniMax_H3_f22_s6_00001_.mp4](results/MiniMax_H3_f22_s6_00001_.mp4)

```
video: h264, 1344x768, 24 fps, 0.917 s (22フレーム)
audio: aac, 32000 Hz, 2ch
```

### 5フレームとの比較

| | 5フレーム | 22フレーム | 倍率 |
|---|---|---|---|
| フレーム数 | 5 | 22 | 4.4× |
| **合計時間** | **98.92 秒** | **108.01 秒** | **1.09×** |
| サンプリング合計 | 24 秒 | 41 秒 | 1.7× |
| 1step あたり | 4.15 秒 | **6.94 秒** | 1.67× |
| ピーク コミット | 49.74 GB (上限 49.7) | **50.56 GB** (上限 51.73) | — |
| ピーク VRAM | 15.11 GB | 15.26 GB | — |
| 最小 利用可能RAM | 1.90 GB | 1.58 GB | — |

**フレーム数4.4倍でも合計時間は1.09倍にしかならない。** モデルのロード(約60秒)が支配的で、
サンプリングは全体の4割弱。フレーム数を増やすほど単位時間あたりの効率は上がる。

### step ごとの時間が単調に伸びる

```
2/6  6.36 s
3/6  6.81 s
4/6  7.01 s
5/6  7.35 s
6/6  7.72 s
```

5フレーム時(4.59 → 4.03 → 3.81 と縮む)と逆の挙動。メモリ圧でスワップが効き始めている可能性が高い。
**フレーム数をさらに増やすとここが急激に悪化する恐れがある**。39フレーム以上を試すときの観測点。

## 3.7 安全装置(ウォッチドッグ)の設計を誤った記録

最初のウォッチドッグは**コミット絶対値 48.5GB** で `/interrupt` を発火させる設計にした。
これは**誤り**で、成功見込みの実行を1回殺した。

```
14,48.77,49.69,2.03,15.07   ← 上限まで 0.92GB 残っていた時点で発火
15,48.63,50.70,2.10,14.13   ← 直後に Windows が上限を 50.70 に拡張していた
```

**コミットが上限付近であること自体は破綻の兆候ではない。** Windows はページファイルを
反応的に拡張して吸収する。実際 5フレーム成功時のピークは 49.74GB / 上限 49.7GB で、
天井に張り付いたまま完走している。22フレーム成功時も 50.56 / 51.73 と上限が追随している。

正しい設計は**物理メモリの枯渇を見ること**:

```powershell
$AVAIL_FLOOR = 0.35   # GB
$STRIKES = 3          # 連続3サンプルで発火
if ($avail -lt $AVAIL_FLOOR) { $strike++ } else { $strike = 0 }
if ($strike -ge $STRIKES) { Invoke-RestMethod -Uri "http://127.0.0.1:8000/interrupt" -Method Post }
```

この設計に直したら、同条件で問題なく完走した。

## 5. 実用構成の計測(2026-08-07) — 3060機の実証構成の移植

構成: 640×640 / 124フレーム(5秒 @24fps) / I2V / Turbo LoRA ckpt500 pruned(strength 1.0)
/ EasyCache(0.3/0.2/0.9) / euler / simple。投入は [run_i2v.py](run_i2v.py)。

### 5.1 4 steps — 成功

| 項目 | 実測 |
|---|---|
| **合計(ロード込み)** | **150.49 秒** |
| モデル初期化 | 38 秒 |
| サンプリング | 55 秒(EasyCache が 4 中 1 step スキップ、1.33x) |
| ピーク コミット | 54.45 GB(上限 55.51 GB まで反応的拡張) |
| **最小 利用可能RAM** | **0.74 GB** ← 危険水域だった |
| 出力 | [results/MiniMax_H3_f124_s4_lora_ec_euler_00001_.mp4](results/MiniMax_H3_f124_s4_lora_ec_euler_00001_.mp4)(音声つき 5.17秒) |

**3060機の約195秒に対し150秒(ロード込み)**。ウォームならさらに縮む見込みだったが——

### 5.2 8 steps — 成功(再計測)

初回のウォーム連続投入はクラッシュ1を引き起こした(冒頭の教訓)。ガード整備後の再計測:
**313.15秒(ロード込み)**、EasyCache 2/8 スキップ、約19.2s/it。
出力: [results/MiniMax_H3_f124_s8_lora_ec_euler_00001_.mp4](results/MiniMax_H3_f124_s8_lora_ec_euler_00001_.mp4)

### 5.3 25 steps / LoRA無し / res_multistep — **この機では物理RAM超えで不可(結論)**

2回試行。1回目はクラッシュ2(強制電源断)。ガーディアン整備後の2回目は、
エアバッグ4段(計24GB)がコミット側を支えたが、**物理メモリ空き 0.35GB** に達して
キルスイッチが ComfyUI を即殺(**PC は無傷** — 防御は設計どおり機能)。

キルの引き金がコミットではなく物理だった点が本質:
weight streaming の作業領域 + 124f latent + res_multistep の履歴バッファが物理RAM 31.7GB を
純粋に超えている。エアバッグはコミット枯渇には効くが物理の絶対量は救えない。
**この構成はこの機では追わない。** 実運用本命(Turbo LoRA 4/8 steps euler)は完走済みなので実害小。

### 5.4 flf2v(first+last frame) — 成功。**キーフレーム補間パイプラインの核が実証された**

`Cityscape_NY.jpg` → `cityscape_004.jpg`(異なる都市写真間のトランジション)、
124f / 4 steps / Turbo LoRA / euler。**208.79秒**で完走、音声つき 5.17秒。
出力: [results/MiniMax_H3_f124_s4_lora_ec_euler_flf2v_00001_.mp4](results/MiniMax_H3_f124_s4_lora_ec_euler_flf2v_00001_.mp4)

`MiniMaxH3ImageToVideo` の `last_frame` 入力に画像を挿すだけ(run_i2v.py の第6引数)。
これで「MJ 等のキーフレーム2枚 → H3 が間を音声つきで補間」が実機で回ることを確認。
複数キーフレームはセグメント連結(kf1→kf2, kf2→kf3)で対応する。

1回目の試行は VAEデコード段でキル(ガーディアン v2 の弾切れ)。v3(回転式バッファ)で解決:
```
15:21〜22  ロード段の波: 展開4回 ↔ 合間に買い戻し(6→12GB回復)
15:23〜24  デコード段の波: 展開2回 ← v2ではここで弾切れだった
結果: キル発火なし、完走
```

## 6. Turbo LoRA 3種の同シード比較(2026-08-10)

条件固定: 640×640 / 124f / 4 steps / seed 12345 / Cityscape_NY.jpg / EasyCache 0.3。

| | LoRA | strength | sampler | 時間(ロード込み) | 出力 |
|---|---|---|---|---|---|
| A | drbaph v1 ckpt500(larryvrh v1 の pruned 変換) | 1.0 | euler | 150.49s (8/7) | `..._euler_00001_` |
| B | **Kijai LightX2V** 蒸留 | 0.75 | er_sde | 243.30s | `..._er_sde_fl2v_lightx2v..._st0.75_` |
| C | **v4_step600**(larryvrh 8/8 更新の pruned 変換) | 1.0 | euler | 198.35s | `..._turbo_v4_step600_st1_` |

**判定(目視、2026-08-10): B の Kijai LightX2V が音・動き・ディテールすべてで最良 → 既定構成に採用。**
run_i2v.py の既定値を LightX2V @0.75 / er_sde に変更済み。

- Kijai 版(_comfy 変換)は pruned fp8 モデルに**キー警告なしで適用できた**
- er_sde は euler よりやや遅い(B の +45s はサンプラー差 + ロード変動の合算)
- **品質判定は目視待ち**(動き・ディテール・4step 特有の音声ノイズを比較)
- 見送り: ComfyUI-Spectrum-MiniMax-H3(スペクトル予測でステップスキップするカスタムノード)。
  20step 帯で2割速化の設計で 4step 運用には効かず、スナップショットでメモリ +2.2GB は
  この機の物理RAM事情に逆行するため。高step品質路線をやる場合に再検討

## 4. 未検証(次にやるなら)

- **39フレーム以上**。22フレーム(0.92秒)までは通った。step 時間が単調に伸びている(3.6節)ので、
  次で急激に悪化する可能性がある。**39 → 56 → 73 と上げて破綻点を特定する**のが次の一手。
  実用長の73フレーム(約3秒)が通れば実用ライン。
- **連続実行時のベースライン管理**。OS再起動直後 22.3GB → 数回の実行後 29.6GB とベースラインが
  上がっていく。`/free` では ComfyUI 分しか戻らない(カーネルプール・vmmem は戻らない)。
  長時間の検証セッションでは定期的な OS 再起動が要る。
- 解像度を下げた場合の軽さと品質。
- steps を実用値(20)に戻したときの時間。単純計算で +56秒程度だが未確認。
- 生成物の**品質評価は一切していない**。5フレーム0.2秒では判断不能。
- ref2va(reference-to-video)は未DL(+21GB)。
